%table
  %tr
    %th Title
    %th Rating
    %th User
  - for game in @games
    %tr
      %td= h game.title
      %td= h @ratings.at(game.rating)
      %td= h game.user
      %td= link_to 'Show', game

%p= will_paginate @games
%p= link_to "New Game", new_game_path
